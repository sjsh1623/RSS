spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rss_prod
    username: root
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  port: 8080 