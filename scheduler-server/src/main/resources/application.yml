spring:
  datasource:
    url: jdbc:mysql://db:3306/katchup
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true 